FROM node:22-alpine

WORKDIR /app

# Only copy what the realtime server needs
COPY realtime-server.cjs ./

RUN npm init -y && npm install ws

RUN addgroup --system --gid 1001 nodejs && \
    adduser --system --uid 1001 rtserver

USER rtserver

EXPOSE 3001

CMD ["node", "realtime-server.cjs"]
