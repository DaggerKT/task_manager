// Re-export from centralised types folder — kept for backward compatibility
export type { ProjectMember, ProjectInfo } from "@/types/project";
export type { Task, TaskComment, Column, ColorOption } from "@/types/kanban";
